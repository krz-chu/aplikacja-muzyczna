<Window x:Class="aplikacja_muzyczna.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:aplikacja_muzyczna"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <StackPanel Margin="10" Grid.Row="3" Grid.Column="1">
            <TextBlock FontSize="15" Name="CurrentFileText" Text="No file selected" HorizontalAlignment="Center" />
            <Label FontSize="15" Name="lblStatus" Content="00:00 / 00:00" HorizontalContentAlignment="Center" />
            <WrapPanel HorizontalAlignment="Center">
                <Button FontSize="15" Name="btnPlay" Click="btnPlay_Click" Margin="5">Play</Button>
                <Button FontSize="15" Name="btnNext" Click="btnNext_Click" Margin="5">Next</Button>
            </WrapPanel>
        </StackPanel>

        <DataGrid x:Name="FilesList"
                  Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="FilesList_MouseDoubleClick"
                  
                  >
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Cover" Width="48">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding CoverSmall}" Width="32" Height="32" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Długość" Binding="{Binding DisplayDuration}" Width="auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Wykonawca" Binding="{Binding Performer}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        

        <StackPanel Grid.Row="3" Grid.Column="0">
            <Button FontSize="15" HorizontalAlignment="Left" Name="btnOpenAudioFileButton" Click="btnOpenAudioFile_Click" Margin="5">Open Audio Folder</Button>
            

            <TextBlock x:Name="TotalFilesText" Margin="5,0" VerticalAlignment="Center" />
            <TextBlock x:Name="LengthFilesText" Margin="5,0" VerticalAlignment="Center" />
            
        </StackPanel>
        

        <StackPanel Grid.Column="2" Grid.Row="2" Grid.RowSpan="3" Margin="5" VerticalAlignment="Top">
            <WrapPanel Width="260" HorizontalAlignment="Left">
                <Button Margin="5" Content="alfabetycznie" Click="btnsort1"/>
                <Button Margin="5" Content="alfabetycznie od tyłu" Click="btnsort2"/>
                <Button Margin="5" Content="losowo" Click="btnsort3"/>
            </WrapPanel>
            <WrapPanel Width="260" HorizontalAlignment="Left">
                <Button Margin="5" Name="btnChangeAlbum" Content="Zmień album (aktualny plik)" Click="btnChangeAlbum_Click"/>
                <Button Margin="5" Name="btnPlayAlbum" Content="Odtwórz album" Click="btnPlayAlbum_Click"/>
            </WrapPanel>
            <CheckBox Margin="5" Name="chkSortByAlbum" Content="Sortuj po albumie" />
            <StackPanel Orientation="Horizontal" Margin="5">
                <Slider x:Name="VolumeSlider" Value="50" Maximum="100" Minimum="0" Margin="0,0,8,0" Width="150" ValueChanged="VolumeSlider_ValueChanged"/>
                <TextBlock x:Name="VolumeValueText" VerticalAlignment="Center" Text="50%"/>
            </StackPanel>




        </StackPanel>
        <Border Grid.Row="1" Grid.Column="2" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="3" MaxHeight="140">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="AlbumsPanel" />
            </ScrollViewer>
        </Border>
        <Image Grid.Column="3" Grid.Row="0" Name="NowPlayingCover" VerticalAlignment="Center" HorizontalAlignment="Center" Stretch="Uniform" Margin="5" Source="{Binding NowPlayingCover}" />

        <Grid Grid.Row="3" Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0">
                <TextBlock Name="SubtitleText" Text="Brak napisów" HorizontalAlignment="Center" TextWrapping="Wrap" />

            </ScrollViewer>

            <Button Grid.Row="1" FontSize="15" HorizontalAlignment="Right" Name="btnSubtitle" Click="btnSubtitle_Click" Margin="5">Napisy</Button>
        </Grid>
       
    </Grid>
</Window>
